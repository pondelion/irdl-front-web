import * as React from 'react';
import Plot from 'react-plotly.js';
 

interface Props {}

export const Bar: React.FC<Props> = (props: Props) => {
  return (
    <div></div>
  );
}
 
export default Bar;
